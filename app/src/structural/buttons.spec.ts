import { createButtons } from './buttons';
import { loadMockDom } from '../tests/mocks';

xdescribe('createButtons', () => {
  beforeEach(() => {
    loadMockDom();
  });
  it('', () => createButtons());
});
